<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Review - Fresh Tomatoes</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  
  <h1>Edit Your Review for <%= review.movieId.title %></h1>

  <form action="/reviews/<%= review._id %>?_method=PUT" method="POST">
    <label for="rating">Rating:</label>
    <select name="rating" id="rating" required>
      <option value="5" <%= review.rating === 5 ? 'selected' : '' %>>5 - Excellent</option>
      <option value="4" <%= review.rating === 4 ? 'selected' : '' %>>4 - Good</option>
      <option value="3" <%= review.rating === 3 ? 'selected' : '' %>>3 - Average</option>
      <option value="2" <%= review.rating === 2 ? 'selected' : '' %>>2 - Poor</option>
      <option value="1" <%= review.rating === 1 ? 'selected' : '' %>>1 - Terrible</option>
    </select>
    <br><br>

    <label for="review">Review:</label><br>
    <textarea name="review" id="review" rows="5" cols="50" required><%= review.review %></textarea>
    <br><br>

    <button type="submit">Update Review</button>
    <a href="/movies/<%= review.movieId._id %>">Cancel</a>
  </form>
</body>
</html>